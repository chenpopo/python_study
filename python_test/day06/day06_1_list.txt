1、列表 - list
	1、列表中常用方法 - list api
		help(list)
	2、详解
		1、append()
			作用：在列表的末尾添加新元素
			语法：
				append(obj)
					参数 obj: 表示要添加到列表末尾的对象
					返回值： 无， 但是会修改原来的列表中的值
			示例：
				L = ["旋涡鸣人", "宇智波佐助","旗木卡卡西"]
				L.append("野田新之助")
				结果 : ["旋涡鸣人", "宇智波佐助","旗木卡卡西","野田新之助"]
		2、count()
			作用： 统计某个元素在列表中出现的次数，不是统计list中所有元素个数(使用len()函数统计长度)，而是特定元素个数
			语法：
				count(obj)
					参数obj: 要统计的对象
					返回值： 返回obj在列表中出现的次数
		3、extend()
			作用： 用于在列表末尾一次性增加另一个序列中的多个值
						(用新列表来扩展源列表中的值)
			语法：
				extend(obj)
					参数obj：被追加到后面的另一个序列
					返回值：	无，会改变原列表的内容
		4、insert()
			作用：用于将指定对象插入到列表的指定位置处
			语法：
				L.insert(index,obj)
				参数 index:
				对象 obj: 需要插入的对象	
				返回值：无，会改变原列表中的内容
		5、index()
			作用：从列表中找出某个值第一次匹配到的索引位置
			语法：
				index(obj)
				参数 obj:  要被查找的对象
				返回值：返回该对象对应的索引位置
				注： 如果没有找到的话则抛出异常
		6、pop()
			作用：用于移除列表中的一个元素，默认是最后一个，并返回该元素的值
			语法：
				L.pop([index=-1])
				参数： [index=-1] 可选参数，要移除的列表的元素的索引值，不能超过列表总长度，默认值位-1，表示删除最后一个列表值
				返回值：返回从列表中移除的元素
		7、remove()
			作用：从列表中移除某个值的第一次匹配项
			语法：
				L.remove(obj)
				参数 obj: 要移除的内容
				返回值：无，原列表中第一次匹配到的内容会被删除
		8、reverse()
			作用： 将列表进行反转
			语法：
				L.reverse()
				参数：无
				返回值：无， 会改变列表的值
		9、sort()
			作用：对原列表中元素进行排序
			语法：
				sort(reverse=False)
				参数 reverse: 排序规则，默认位False表示生序，设置为True则为降序
				返回值： 无，会改变原列表中的值
	3、练习
		输入多行文字，保存进列表中，每次输入回车后算一行（输入空行时表示结束）
		1、按原输入内容在终端进行输入
		2、打印输出共有多少行内容
		3、打印输出共输入了多少个字符
		
	4、字符串的文本解析方法
		1、split()
			语法： S.split(sep=None)
			作用： 将字符串S使用sep作为分隔符进行拆分，返回分割后的字符串列表，如果不指定sep的话则采用空白字符作为分隔符
		2、join
			语法： s.join(list)
			作用： 用可迭代对象中的字符串，拼接并返回中间使用s 进行分割的字符串
	5、浅拷贝和深拷贝
		1、浅拷贝 - shollow copy
			1、什么是浅拷贝
				浅拷贝是值在复制过程中，
		2、深拷贝
			1、语法
				import copy
				L1 = copy.deepcopy(L)
	6、列表推导式 - comprehension
		1、什么是列表推导式
			利用可迭代对象生成列表的表达式
			利用简单的方法生成列表
		2、语法
			[表达式 for 变量 in 可迭代对象 if 真值表达式]
			在 可迭代对象范围内的数据，如果满足 if条件，则执行 表达式，并返回结果列表
			注意：
				1、if真值表达式  是可以省略不写的
				2、表达式 是由 变量配合其他运算组合成的一个表达式值
		3、示例：
			#生成一个1-9的平方的列表
			L = [x**2 for x in range(1,10)]
			print(L)

			#生成一个1-100之内的奇数列表
			L = [x for x in range(1, 101) if x % 2 == 0]
			print(L)
			#生成一个数值位1-9的平方列表，并去掉所有的奇数的平方
			L = [x**2 for x in range(1,10) if x % 2 ==0 ]
			print(L)
		4、列表推导式的嵌套
			语法：
				[]
				#列表推导式的嵌套
